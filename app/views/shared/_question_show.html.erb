<strong><%= question.question %></strong>
  <ol>

    <% question.answers.each do |a| %>
      <li><%= a.answer %>, <%= a.diva.name %>
    <% end %>

  </ol>

<%= button_to "Delete Question", question_path(question), :confirm => "Are you sure?", :method => :delete, :class => "btn btn-danger" %>